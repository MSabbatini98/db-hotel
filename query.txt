1) Seleziona tutti gli ospiti che sono stati identificati con la carta di identità

SELECT * 
FROM `guest`
WHERE identification = CID;


2) Seleziona tutti gli ospiti che sono nati dopo il 1988

SELECT *
FROM `guest`
WHERE `dob` > `01-01-1998`


3) Seleziona tutti gli ospiti che hanno più di 20 anni (al momento dell’esecuzione della query)

SELECT * 
FROM `guest`
WHERE PERIOD_DIFF(NOW(), `dob`);


4) Seleziona tutti gli ospiti il cui nome inizia con la D
SELECT * 
FROM `guest`
WHERE name LIKE `D%`;


5) Calcola il totale degli ordini accepted

SELECT SUM(price) 
FROM `guest`
WHERE status LIKE `accepted`;


6) Qual è il prezzo massimo pagato?
SELECT MAX(price)
FROM `guest`

7) Seleziona gli ospiti riconosciuti con patente e nati nel 1975
SELECT * 
FROM `guest`
WHERE identification = patente 
AND `yob` = 1975;

8) Quanti posti letto ha l’hotel in totale?

SELECT SUM(beds) AS totale_letti
FROM `hotels`